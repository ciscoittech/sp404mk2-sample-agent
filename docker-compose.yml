version: '3.8'

services:
  # PostgreSQL Database Service
  postgres:
    image: postgres:16-alpine
    container_name: sp404-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-sp404_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme123}
      POSTGRES_DB: ${POSTGRES_DB:-sp404_samples}
      POSTGRES_INITDB_ARGS: "-E UTF8 --locale=en_US.UTF-8"
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backend/scripts/init_postgres.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-sp404_user} -d ${POSTGRES_DB:-sp404_samples}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - sp404-network
    labels:
      - "com.orbstack.domain=postgres"

  # FastAPI Backend Service
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sp404-backend
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-sp404_user}:${POSTGRES_PASSWORD:-changeme123}@postgres:5432/${POSTGRES_DB:-sp404_samples}
      API_HOST: ${API_HOST:-0.0.0.0}
      API_PORT: ${API_PORT:-8100}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
      ENVIRONMENT: ${ENVIRONMENT:-development}
    ports:
      - "${API_PORT:-8100}:8100"
    volumes:
      - ./backend:/app/backend:delegated
      - ./samples:/app/samples:ro
      - backend-uploads:/app/backend/uploads
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - sp404-network
    command: uvicorn app.main:app --host 0.0.0.0 --port 8100 --reload
    labels:
      - "com.orbstack.domain=api"

volumes:
  postgres-data:
    driver: local
  backend-uploads:
    driver: local

networks:
  sp404-network:
    driver: bridge

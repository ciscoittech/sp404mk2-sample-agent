import { defineConfig } from 'vite'
import { resolve } from 'path'

export default defineConfig({
  build: {
    rollupOptions: {
      input: {
        main: resolve(__dirname, 'index.html'),
        samples: resolve(__dirname, 'pages/samples.html'),
        kits: resolve(__dirname, 'pages/kits.html'),
        batch: resolve(__dirname, 'pages/batch.html'),
      }
    }
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  }
})